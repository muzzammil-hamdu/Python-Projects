trigger:
- main

pool:
  vmImage: ubuntu-latest

variables:
  imageName: python-project
  acrLoginServer: devopsacr123456.azurecr.io
  resourceGroup: aks-dev-rg
  aksCluster: dev-aks-cluster
  tag: $(Build.BuildId)

stages:

# ================= BUILD STAGE =================

- stage: Build
  displayName: Build and Push Docker Image
  jobs:
  - job: Build
    steps:

    - checkout: self

    - task: Docker@2
      displayName: Build and Push Image to ACR
      inputs:
        containerRegistry: acr-service-connection
        repository: $(imageName)
        command: buildAndPush
        Dockerfile: Dockerfile
        tags: |
          $(tag)
          latest

# ================= DEPLOY STAGE =================

- stage: Deploy
  displayName: Deploy to AKS
  dependsOn: Build
  jobs:
  - job: Deploy
    steps:

    - checkout: self

    - script: |
        ls -R
      displayName: Show Repository Files

    - task: Kubernetes@1
      displayName: Deploy Deployment
      inputs:
        connectionType: Azure Resource Manager
        azureSubscriptionEndpoint: azure-service-connection
        azureResourceGroup: $(resourceGroup)
        kubernetesCluster: $(aksCluster)
        namespace: default
        command: apply
        useConfigurationFile: true
        configuration: k8s/deployment.yaml

    - task: Kubernetes@1
      displayName: Deploy Service
      inputs:
        connectionType: Azure Resource Manager
        azureSubscriptionEndpoint: azure-service-connection
        azureResourceGroup: $(resourceGroup)
        kubernetesCluster: $(aksCluster)
        namespace: default
        command: apply
        useConfigurationFile: true
        configuration: k8s/service.yaml
